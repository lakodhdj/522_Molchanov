<Page x:Class="_522_Molchanov.Pages.PaymentTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="PaymentTabPage">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- DataGrid для отображения платежей -->
        <DataGrid Grid.Row="0" x:Name="DataGridPayment"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,0,10"
                  SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date}" Width="80"/>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding User.FIO}" Width="150"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Num}" Width="80"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Price}" Width="80"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category.Name}" Width="150"/>
                <DataGridTemplateColumn Header="Редактировать">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" 
                                    Click="ButtonEdit_Click"
                                    Style="{StaticResource ButtonStyle}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="ButtonAdd" Content="Добавить" Width="120" Margin="10" 
                    Click="ButtonAdd_Click" Style="{StaticResource ButtonStyle}"/>
            <Button x:Name="ButtonDel" Content="Удалить" Width="120" Margin="10" 
                    Click="ButtonDel_Click" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>
